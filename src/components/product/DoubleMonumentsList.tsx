"use client";

import MonumentsList from "./MonumentsList";

const DoubleMonumentsList: React.FC = () => {
  return <MonumentsList category="double" title="Подвійні пам`ятники" />;
};

export default DoubleMonumentsList;
